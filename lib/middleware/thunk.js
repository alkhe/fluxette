"use strict";

// export default Interface =>
// 	class extends Interface {
// 		interop(actions, update) {
// 			for (let i = 0; i < actions.length; i++) {
// 				let action = actions[i];
// 				if (action instanceof Function) {
// 					actions = action(interop);
// 				}
// 			}
// 			super.interop(actions, update);
// 		}
// 	}